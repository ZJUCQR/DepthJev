line-length = 120
target-version = "py311"
extend-exclude = ["repos", "logs"]

[lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]

[lint.per-file-ignores]
# These modules also run in the Python 3.9 evaluation environment.
"depthjev/config.py" = ["UP", "B905"]
"depthjev/eb_launch.py" = ["UP", "B905"]
"depthjev/report.py" = ["UP", "B905"]
